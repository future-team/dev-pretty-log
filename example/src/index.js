/**
 * Created by slashhuang on 16/3/3.
 */
import devLog from '../../src/index.js';
devLog.log('1st param','2nd param');
devLog.warn({a:1,b:{c:2}});
devLog.debug('debugging');
devLog.error('error');
devLog.dir('warn',{a:1,b:{c:2}});

